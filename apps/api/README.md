# API

API NestJS del asistente de ventas. Se ejecuta como paquete del workspace y no
debe instalar dependencias de forma aislada.

Desde la raíz del repositorio:

```bash
pnpm --filter @buscador-rag/api dev
pnpm --filter @buscador-rag/api test
pnpm --filter @buscador-rag/api build
```
